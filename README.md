# email-extractor-tool
Email Extractor Tool  A Python-based tool that scans text files, documents, or pasted content and extracts valid email addresses, removes duplicates, and saves them into a clean file. What This Tool Will Do  Accept text file OR pasted text  Extract all emails  Remove duplicates  Validate format  Save result to emails.txt  Show total count
